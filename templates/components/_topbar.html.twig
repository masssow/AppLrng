<header class="fixed top-0 left-0 right-0 z-30 h-14 bg-[#0A0A0F] border-b border-white/10 flex items-center justify-between px-4 md:pl-68">
    <span class="font-bold text-[#6C63FF] text-lg">AcquisApp</span>
    <div class="flex items-center gap-3">
        {% include 'components/_lang_switcher.html.twig' %}
        <a href="{{ path('app_logout') }}" class="text-sm text-white/60 hover:text-white">
            {{ 'nav.logout'|trans }}
        </a>
    </div>
</header>
